pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            agent {
                docker {
                    image 'snakee/golang-junit:1.21'
                    reuseNode true 
                }
            }
            steps {
                dir('bugtracker-backend') {
                    withEnv(["GOCACHE=/var/jenkins_home/workspace/BugTracker pipeline/.go-cache"]) {
    sh 'go test -v ./...'
                }
            }
        }
    }
  }
}