pipeline {
    agent {
        docker {
            image 'snakee/golang-junit:1.21'
            reuseNode true
            args '-u root'
        }
    }
    stages {
        stage('Test') {
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'  // Теперь работает как root
                }
            }
        }
    }
}